<!--
  Empty on purpose: the chidori mark (ring + lightning bolts + "xdutsuay",
  taken from the sister lclreason desktop app's appicon.png) is baked into
  ic_launcher_background as a single flat image, so this layer stays
  transparent rather than doubling up content on top of it.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108" />

