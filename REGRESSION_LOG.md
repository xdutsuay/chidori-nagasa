# Regression Log

Required by `CHIDORI_PROTOCOL.md` §3.3. One entry per bug that reached
`main`, tracking root cause and the regression test added so it can't
silently come back. Empty at repo creation — first entries land with the
first bugfix PRs.

| Date | Bug | Root cause | Regression test added | PR |
|------|-----|------------|------------------------|----|
| — | — | — | — | — |
